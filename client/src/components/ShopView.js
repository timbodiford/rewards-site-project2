import React, { Component } from 'react'
import { userInfo } from 'os';

export default class ShopView extends Component {
    render() {
        return (
            <div>
                {/* <h2>Welcome, {user.firstName} {user.lastName}</h2>
                <h5>You have {user.pointsBalance} points!</h5> */}
            </div>
        )
    }
}
